# Session Handover

## 1. Objective
- Main goal: Start core development Step 1 by implementing shared `inject` core logic for LaMian.
- Scope for next session: complete `inject` path (validation + persistence + CLI wiring + tests) in small, verifiable slice.

## 2. Current Status
- Completed:
  - Planning and governance docs for Project 4 are in place.
  - Rust CLI bootstrap exists in `project4/lamian/` with working `init` and SQLite migrations.
  - Ingest architecture is locked: one shared ingest core for CLI and GUI drag-and-drop.
  - Context-window and handover discipline rules were added to AGENTS/CLAUDE docs.
- In progress:
  - `inject` command is defined in CLI but still returns "not implemented yet".
- Blockers:
  - No hard blocker for Step 1.
  - Open product decisions (GUI framework, sidecar default format, dedup policy) are not blocking `inject` core.

## 3. Key References
- `project4/TODO.md`: active task list and status (`P4-103`, `P4-104`, `P4-105` are next).
- `project4/SPEC.md`: canonical requirements and ingest architecture rule.
- `project4/DECISIONS.md`: ADR-006 locks shared ingest core across CLI and GUI.
- `project4/lamian/src/cli.rs`: command and argument definitions.
- `project4/lamian/src/commands.rs`: dispatch; `inject` currently stubbed.
- `project4/lamian/src/db.rs`: schema/migrations and vault initialization.
- `project4/lamian/src/error.rs`: typed error enum for CLI/domain failures.
- `project4/lamian/docs/SPEC.md`: standalone implementation spec snapshot.

## 4. Decisions
- Decision: Keep one ingest core service used by both CLI `inject` and GUI drag-and-drop.
- Rationale: Avoid logic drift and enforce provenance/data rules consistently.

## 5. Verification
- Ran:
  - `cd project4/lamian && cargo test -q`
- Result:
  - 2 tests passed (`init` + migration idempotency).
- Not run yet:
  - `cargo clippy --all-targets -- -D warnings` after next code changes.
  - `cargo fmt --all --check` after next code changes.
- Reason:
  - This handover session only updated context snapshot files.

## 6. Next Steps
1. Implement domain-level `inject` service in Rust with strict provenance validation and `Result`-based errors.
2. Wire `inject` CLI handler to call the shared service and persist `figures` + `sources` in one transaction.
3. Add tests for success path and validation failures (missing source fields, file missing).

## 7. Resume Commands
```bash
cd /Users/mac/Dropbox/work/project/vibe/yuzhe

git branch --show-current
git status --short --branch

cd project4/lamian
cargo run -- --help
cargo run -- --vault /tmp/lamian_vault_dev init

# after implementing inject
cargo fmt --all
cargo clippy --all-targets -- -D warnings
cargo test
```

## 8. Risks / Open Questions
- Risk: duplicate/hash policy for identical files is not finalized yet.
- Risk: provenance UX for GUI drop will need metadata prompt flow, but this is post-core wiring.
- Open question: sidecar default (`yaml` vs `json`) remains unresolved.
