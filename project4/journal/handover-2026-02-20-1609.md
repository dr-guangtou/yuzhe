# Session Handover

## 1. Objective
- Main goal: complete LaMian core Step 1 by implementing the shared `inject` path and real-case validation.
- Scope for next session: start the next core slice by implementing `tag` behavior (`add` first), then expand to `remove`/`rename`.

## 2. Current Status
- Completed:
  - Implemented shared ingest core in `project4/lamian/src/inject.rs` with strict provenance validation, typed errors, and deterministic `figure_id`.
  - Wired CLI `inject` dispatch in `project4/lamian/src/commands.rs`.
  - Added typed ingest-related errors in `project4/lamian/src/error.rs`.
  - Added integration tests in `project4/lamian/tests/cli_inject_real.rs`:
    - positive real fixtures path
    - negative wrong-format fixture path
  - Added fixture set in `project4/lamian/tests/fixtures/` and documented policy/checklist in `project4/lamian/tests/fixtures/README.md`.
  - Merged feature branch `feat/lamian-inject-core-step1` into `main` (fast-forward).
- In progress:
  - `update`, `tag`, `link`, `search`, `export` remain `NotImplemented` stubs.
  - End-to-end CLI workflow tests beyond inject are pending.
- Blockers:
  - No technical blocker.
  - Non-code caution: redistribution rights for `2602.17205_1.png` should be verified before public release.

## 3. Key References
- `project4/TODO.md`: project-level backlog and review notes.
- `project4/lamian/docs/TODO.md`: implementation-level status (`L-104`, `L-105` pending/in-progress).
- `project4/lamian/src/inject.rs`: shared ingest core service.
- `project4/lamian/src/commands.rs`: CLI command dispatch and remaining stubs.
- `project4/lamian/src/error.rs`: typed error contracts.
- `project4/lamian/tests/cli_inject_real.rs`: CLI integration tests for real fixtures.
- `project4/lamian/tests/fixtures/README.md`: fixture provenance + future format checklist.

## 4. Decisions
- Decision: keep real image fixtures inside repository under `project4/lamian/tests/fixtures/` for deterministic CLI integration tests.
- Rationale: removes local-path dependency (Desktop/Downloads) and keeps tests reproducible.

## 5. Verification
- Ran:
  - `cd project4/lamian && cargo fmt --all && cargo clippy --all-targets -- -D warnings && cargo test`
- Result:
  - Passed (`6` unit tests + `2` integration tests).
- Not run yet:
  - Any tests for `tag`, `link`, `search`, `update`, `export` command behavior.
- Reason:
  - Those command paths are not implemented yet.

## 6. Next Steps
1. Implement `tag add` with normalization + validation in shared core and CLI dispatch.
2. Add tests for `inject -> tag add` happy path and duplicate/invalid tag failures.
3. Implement `tag remove` and `tag rename`, then expand integration tests.

## 7. Resume Commands
```bash
cd /Users/mac/Dropbox/work/project/vibe/yuzhe

git branch --show-current
git status --short --branch

cd project4/lamian
cargo run -- --help

# after tag implementation changes
cargo fmt --all
cargo clippy --all-targets -- -D warnings
cargo test
```

## 8. Risks / Open Questions
- Open question: retain or replace non-public-domain fixture files before making repository public.
- Risk: tag normalization rules should be finalized early to avoid migration/refactor later.
