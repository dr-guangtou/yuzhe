# Session Handover

## 1. Objective
- Main goal: Finalize a decision-complete implementation plan for Phase 1.8 (hardening + CLI expansion) before coding.
- Scope for next session: Execute Phase 1.8 in prioritized waves, starting from critical correctness fixes.

## 2. Current Status
- Completed:
  - Reviewed current codebase and produced a detailed risk/gap review.
  - Ingested independent review findings and mapped valid items into internal backlog IDs.
  - User decisions were locked:
    1. Downgrade Rust edition to 2021.
    2. Implement staged-file + journal design now.
    3. Allow filterless saved queries by default.
    4. Add global `--json` flag across all commands.
  - Produced a decision-complete Phase 1.8 plan in chat.
- In progress:
  - Planning artifacts updated locally but not committed.
- Blockers:
  - None technical; next step is implementation execution.

## 3. Key References
- `project4/PLAN.md`: Phase 1.8 hardening wave scope and sequencing notes.
- `project4/TODO.md`: project-level backlog with P4-401..P4-430.
- `project4/lamian/docs/TODO.md`: standalone backlog with L-171..L-200.
- `project4/lamian/docs/SPEC.md`: needs Phase 1.8 CLI/output/model updates.
- `project4/lamian/src/tag.rs`: BUG-2 rename corruption area.
- `project4/lamian/src/link.rs`: BUG-3 self-link removal guard area.
- `project4/lamian/src/bundle.rs`: staged-file+journal redesign target.
- `project4/lamian/src/cli.rs`: global `--json` and new command flags.
- `project4/lamian/src/commands.rs`: output-mode unification and command wiring.
- `project4/lamian/src/query.rs`: allow filterless saved queries.
- `project4/lamian/Cargo.toml`: edition downgrade to 2021.

## 4. Decisions
- Decision: Rust edition policy -> use `edition = "2021"`.
- Rationale: maximize toolchain compatibility without requiring newest stable.
- Decision: Bundle hardening -> staged-file + journal now.
- Rationale: close crash-consistency and partial-write recovery risks immediately.
- Decision: Query semantics -> filterless saved queries allowed.
- Rationale: supports useful sort+limit presets.
- Decision: Output contract -> global `--json` across all commands.
- Rationale: automation parity and consistent machine-readable CLI behavior.

## 5. Verification
- Ran:
  - Repo exploration and static code/doc review only for planning.
- Result:
  - No implementation run in this planning segment.
- Not run yet:
  - `cargo fmt --all`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo test`
- Reason:
  - No new code implementation executed yet in this planning turn.

## 6. Next Steps
1. Create execution branch and implement Wave 1 critical fixes first.
   - `git checkout -b feat/lamian-phase-1-8-wave-1`
2. Implement and test:
   - BUG-2 tag rename plan-before-mutate fix.
   - BUG-3 allow self-link cleanup in `link remove`.
   - Edition downgrade to 2021 + toolchain policy file.
3. Run gate and update docs/spec/todo statuses after each wave.
   - `cd project4/lamian && cargo fmt --all && cargo clippy --all-targets -- -D warnings && cargo test`

## 7. Resume Commands
```bash
cd /Users/mac/Dropbox/work/project/vibe/yuzhe
git status --short --branch
git checkout -b feat/lamian-phase-1-8-wave-1

# Implement wave-1 changes, then run:
cd project4/lamian
cargo fmt --all
cargo clippy --all-targets -- -D warnings
cargo test
```

## 8. Risks / Open Questions
- Global `--json` envelope shape must be fixed early to avoid churn in downstream automation/tests.
- Bundle journal recovery semantics need exact state-machine tests to avoid ambiguous restart behavior.
- Link uniqueness migration (v5) must include deterministic dedupe policy for existing rows.
