[project]
name = "journal-summary"
version = "0.1.0"
description = "Periodic journal summary for astronomy research"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "feedparser>=6.0",
    "sentence-transformers>=2.2",
    "numpy>=1.24",
]
