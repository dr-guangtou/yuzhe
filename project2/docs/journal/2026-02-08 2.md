# 2026-02-08: Initial Implementation

## What was done
- Created full project2 pipeline: ORCID fetch -> ADS enrich -> YAML DB -> LLM summaries -> portfolio builder
- All 10 source files implemented and tested end-to-end
- Copied llm_client.py from project1 (works unmodified with adapted config.py)
- Full run: 37 publications from ORCID, 36 enriched via ADS, 36 summarized (4 LLM calls each = 144 calls)

## Key decisions
- ADS enrichment via DOI-first, arXiv-fallback strategy
- Database merge uses DOI (primary) and normalized title (fallback) matching
- Summary YAML files organized by model name under summary/{model}/
- Portfolio slugs: {year}_{last_name}_{short_title}

## Issues encountered
1. Initial fallback chain (moonshot, nvidia, gemini) had no API keys set. Fixed to use kimi, qwen, glm, openai.
2. One publication ("A MaNGA about the Legacy...") not found on ADS - likely too new. Skipped gracefully.
3. In debug mode, pipeline summarizes all stored publications (not just newly fetched). This is by design.

## Verification results
- Fetch-only mode: 37 publications fetched and stored correctly
- Update detection: second run correctly identifies 0 new publications
- Full pipeline: 36/37 papers summarized (1 skipped - no abstract). All 4 summary YAML files created.
- Chinese translations read naturally with technical terms preserved
- Portfolio Markdown documents well-formatted with links, citations, summaries
